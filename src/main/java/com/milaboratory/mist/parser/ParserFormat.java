package com.milaboratory.mist.parser;

/**
 * String format for parser:
 * "SIMPLIFIED": parsers and filters with their arguments in parentheses, as in their toString() methods
 * "NORMAL": normal string format for end users
 */
public enum ParserFormat {
    SIMPLIFIED,
    NORMAL
}
