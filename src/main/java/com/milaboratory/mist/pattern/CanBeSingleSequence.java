package com.milaboratory.mist.pattern;

public interface CanBeSingleSequence {
    /**
     * Returns true if this pattern searches for single sequence without big gaps.
     */
    boolean isSingleSequence();
}
